<?xml version="1.0" encoding="utf-8"?>

<layout>

    <data>

        <import type="android.view.View" />

        <variable
            name="travel"
            type="com.yun.mysimpletravel.ui.home.travel.TravelViewModel" />
    </data>


    <androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:id="@+id/layout_parent"
        android:layout_height="match_parent"
        android:background="@color/white">

        <TextView
            android:id="@+id/tv_weather"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/black"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            android:visibility="@{travel.isWeatherLoading ? View.INVISIBLE : View.VISIBLE}"
            app:layout_constraintStart_toEndOf="@id/iv_weather"
            app:layout_constraintTop_toTopOf="parent" />

        <ImageView
            android:id="@+id/iv_weather"
            android:layout_width="40dp"
            android:layout_height="40dp"
            app:layout_constraintBottom_toBottomOf="parent"
            android:visibility="@{travel.isWeatherLoading ? View.INVISIBLE : View.VISIBLE}"
            app:layout_constraintEnd_toStartOf="@id/tv_weather"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <ProgressBar
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintTop_toTopOf="@id/tv_weather"
            app:layout_constraintBottom_toBottomOf="@id/tv_weather"
            app:layout_constraintStart_toStartOf="@id/iv_weather"
            android:visibility="@{travel.isWeatherLoading ? View.VISIBLE : View.INVISIBLE}"
            app:layout_constraintEnd_toEndOf="@id/tv_weather"
            android:indeterminateTint="@color/color_FBA53B" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</layout>